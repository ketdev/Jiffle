#include "jiffle.h"

namespace jiffle {

	std::vector<frame> generate(const std::vector<data>& modules) {
		std::vector<frame> frames;

		// create frames per module
		for each (auto module in modules)
		{

		}



		return frames;
	}

	
}